"""demo/seed — the INVENTED demo data: schema, runtime.sql install, and the
deterministic generator (T-2-plan.md §5, W5).

Every row this package produces is fabricated from fixed literal seeds.
Nothing in it is, or is derived from, real data.
"""
