# T-1 — the two verifications the owner made his ruling conditional on

**Written 2026-08-21.** This document assembles two verification passes and the two adversarial
re-checks that followed them. It is not a new investigation; it is the record of the two bounded
items the ticket was sent back for.

> ### Reading note added 2026-08-21 — every `FINDINGS.md:NNNN` below is a *pre-amendment* line number
>
> This document was written **before** `FINDINGS.md` was amended, and it is deliberately left that way:
> several of its findings quote what a line *used to* say, so rewriting the numbers in place would
> falsify the record. Two rounds of edits have since been inserted into `FINDINGS.md` — the three-item
> **amendment round** this document asked for (2026-08-21, under go-ahead `GA-3`) and a follow-on
> **parts-reconciliation pass** that scoped two more sites. The file grew from 5,389 lines to 5,528.
>
> To convert any line number below into a current one, add the offset for its band:
>
> | pre-amendment line | add | why |
> | ---: | ---: | --- |
> | 1 – 928 | **0** | untouched |
> | 929 – 1010 | **+16** | `f2` summary-row scope note |
> | 1011 – 1125 | **+27** | + §2.4 heading scope note |
> | 1126 – 1146 | **+41** | + C3 `or`/`and`/`not` correction note |
> | 1147 – ~4849 | **+54** | + C4 depth correction note |
> | ~4850 – 5389 | **+58** | + §5.9 item-1 scope note |
>
> (The §5.9 item-1 paragraph was reflowed, so lines inside roughly 4846–4849 do not map one-to-one.)
> Worked examples of the citations this document leans on hardest:
>
> | cited here | now at | what is there now |
> | --- | --- | --- |
> | `:925` | **:925** | same line — but the row is now **scoped** to value-domain kind probes, so §3.2's "still reads an unscoped 403 / 403" is **spent** |
> | `:1121` / `:1124` | **:1148** / **:1151** | the C3 table; the row now reads **333 / 333 / 332**, so §3.2's "still publishes 400 / 334 / 499" is **spent** |
> | `:1143-1144` | **:1184-1185** | the depth sentence now reads **63**, so §3.2's "still says 64" is **spent** |
> | `:1330` | **:1384** | `f2`'s compliance block, unchanged text |
> | `:2116` | **:2170** | the 130-case strict-jsonpath claim, unchanged |
> | `:2875` | **:2929** | `f4` §4.10 item 10, the `+2.2%` concession, unchanged |
> | `:5192` / `:5193` | **:5250** / **:5251** | the "performance leg / fixture-adequacy leg — firmer" rows, unchanged |
>
> **Three of this document's findings were acted on and are no longer live** — the two `f2` material
> errors (§3.2) and the unscoped 403 / 403 row. Everything else it records still stands. `FINDINGS.md`
> was still being edited when this note was written, so re-derive before quoting a number.

---

## The answer up front

**The NO-GO recommendation still stands on this evidence, but it stands on one fewer leg than
`FINDINGS.md` claims for it.** Item 1 came back clean and in doing so took away a stated NO-GO
argument; Item 2 reproduced every retained measurement exactly, found two real holes in the
document's account of its own process, and was then itself judged OVERSTATED by an adversarial
re-check in three places, including its one headline weakening finding.

Fuller version: the rig is sound — the conformance harness demonstrably reports failures when driven,
so the 130/130 headline is a credible fact rather than a possibly-hollow one, which *removes* the
ground `FINDINGS.md:5193` gives for calling the fixture-adequacy leg "firmer". Nothing found in
either item touches the three facts the ruling actually turns on (no fallback channel in `resolve()`;
3.79×–7.15× slower; 18 of 33 divergence classes undetectable in principle). The result that *would*
have overturned the ruling — the harness scoring `DID_NOT_COMPILE` as a pass, voiding every
downstream conformance number — is the one the pass did not get.

**Before signing, read §3.2** (a closure seat died mid-pass, `FINDINGS.md` does not say so, and two
of the six corrections it dropped are still wrong in the published text), **§3.3 and §4.2's second
paragraph** (the `+463%` figure — the one argument here for the *other* option; the document does
disclose it, but not at the ten sites that cite it), **and §5.1** (the instrument that was *not*
tested, and it is the one that produced every decision-relevant divergence).

---

## 1. What the owner asked for, and why

Recorded verbatim in `kb/notes/owner-answers.md`, section A.

**Q4 — "Prove the test rig can actually report a failure, before ruling?"**
→ **"Yes — do that run before I rule."**

The reason this question exists: `FINDINGS.md` §5.9(6) states, *against* the spike's own headline,
that the code assigning every outcome other than `COMPILED_AGREES` had never executed. If a harness
scores "did not compile" as a pass, 130/130 is meaningless and so is everything derived from it.
That is the failure mode `FRAMING.md` §8 was written in advance to forbid:

> "A harness that silently scores 'did-not-compile' as a pass would reproduce, inside the spike
> itself, exactly the failure mode the spike exists to rule out."

**Q5 — "Accept the evidence trail as it stands?"**
→ **"Reconstruct the missing reports first."**

The reason: the spike's closure log admits two gaps about itself — a refusal register whose reports
were not retained, and three `probes.json` blocks with no surviving producer script. Both sit under
numbers the document cites.

**What follows from Q4 + Q5.** Both say *"before I rule."* The `sp_decide` gate therefore stayed
uncleared, T-1 went back to `sp-investigate` for exactly these two items, and the ruling remains
**stated, not signed**. Nobody has signed anything on the owner's behalf.

**Q7** — *"Let them edit the existing code in place"* — waived `FRAMING.md` §3's no-edit rule for
these two runs. Everything changed under that waiver is listed in §6.

---

## 2. Item 1 — can the conformance harness report a failure?

**Section file:** `spikes/T-1/.recheck/harness.md` (35 KB, full working).
**Adversarial re-check verdict: STANDS.** A second seat re-ran every load-bearing command
independently and every number reproduced. It found five defects, **all cosmetic wording**, none of
which moves the conclusion. They are folded into what follows rather than reported separately, and
each is marked *[corrected]*.

### 2.1 Result

**The harness IS capable of reporting a failure.** `FINDINGS.md` §5.9(6) is factually correct that
the outcome branches had never executed — that is now confirmed by measured line coverage, not
inferred. But when driven by deliberately wrong compilations, they emit `COMPILED_DIVERGES`,
`DID_NOT_COMPILE` and `SQL_ERROR` correctly, through the real per-case loop, with the failures
carried all the way through to the rendered report.

**The branches were dead, not broken. The `FRAMING.md` §8 failure mode did not occur.**

### 2.2 The structure, read rather than assumed

`proto/conformance.py` (1,078 lines) assigns outcomes at exactly four sites, all inside `run()`'s
per-case loop, with no fallthrough:

```
$ grep -n 'outcome=' conformance.py
378:  entry.update(outcome="DID_NOT_COMPILE", ...)
398:  entry.update(outcome="SQL_ERROR", ...)
444:  entry.update(outcome="COMPILED_AGREES", ...)
451:  outcome="COMPILED_DIVERGES",
```

Four hits in the whole file. Counts are asserted to sum to `len(CASES)` at `:508-512`, and the
pass-rate denominator is the full case count (`:508`, `:535-536`) — so a case cannot vanish from the
denominator.

### 2.3 The branches were dead — measured, not inferred

A full normal run (selftest + all 130 cases) under a line tracer:

```
$ <venv-python> spikes/T-1/.recheck/trace_outcome_lines.py
line 377 hits=0   line 378 hits=0   line 380 hits=0     <- DID_NOT_COMPILE
line 396 hits=0   line 398 hits=0   line 402 hits=0     <- SQL_ERROR
line 443 hits=130 line 444 hits=130 line 445 hits=130   <- COMPILED_AGREES
line 449 hits=0   line 451 hits=0   line 455 hits=0     <- COMPILED_DIVERGES
total distinct lines of conformance.py executed: 466
```

The 23 negative controls do not rescue this: they never drive the per-case loop
(`sed -n '946,1041p' conformance.py | grep -n 'outcome\|counts\|entries\|entry'` → no matches), and
no control in `results.json` records an outcome string.

Lines `447-448` — the "mirrored rule agrees, stricter deep check does not" note inside the AGREES
branch — are also never executed (`hits=0`), which corroborates the report's own statement that no
case passes the mirrored rule while failing the strict deep check. *[corrected: the section
originally called this "a 24-line dead surface, not the 22 lines §5.9(6) implies". §5.9(6) implies no
line count at all — it cites the span `:376-455`, which is 80 lines. By the section's own arithmetic
the dead surface is 28 lines, not 24, since the integrity handler at `:405-408` is likewise dead in a
normal run. The `447-448` measurement itself reproduced exactly.]*

### 2.4 Reproducibility from cold

The spike database was **gone** when this pass started (`FATAL: database "autosql_spike" does not
exist`). It was rebuilt from `proto/runtime.sql`, and the unmodified harness regenerated its
artifacts **byte-identically**:

```
$ diff .recheck/orig/CONFORMANCE.md.orig proto/CONFORMANCE.md && echo IDENTICAL
IDENTICAL
$ sha256sum proto/results.json .recheck/orig/results.json.orig
002cda09c2238c1f2bf38c85ae7fe113b33322e7efa843278b4407aff945c212  proto/results.json
002cda09c2238c1f2bf38c85ae7fe113b33322e7efa843278b4407aff945c212  .recheck/orig/results.json.orig
```

(Re-verified while assembling this document; both hashes still match, and `proto/CONFORMANCE.md`
matches its `.orig` at `72db7f82…`.) All four recorded input hashes — fixture, `expr.py`,
`compile.py`, `runtime.sql` — match what `results.json` recorded. 130 cases, 23 controls, 8 probes,
full rendered report, all identical.

### 2.5 The experiment — six injections through the real loop

`proto/conformance_injection_test.py` (new; see §6) replaces `compile_ast`'s **output** for named
cases and asserts what the harness emits. Full transcript: `.recheck/injection-run.txt`.

```
$ <venv-python> proto/conformance_injection_test.py
```

| # | case | injected | expected | **emitted** |
| --- | --- | --- | --- | --- |
| I1 | `add` (`1 + 2`) | `to_jsonb(999::float8)` — flatly wrong number | `COMPILED_DIVERGES` | **`COMPILED_DIVERGES`** |
| I2 | `precedence_mul_before_add` | compiler raises `Uncompilable` | `DID_NOT_COMPILE` | **`DID_NOT_COMPILE`** |
| I3 | `parens_override` | `to_jsonb((1::int / 0::int))` — raises in PG | `SQL_ERROR` | **`SQL_ERROR`** |
| I4 | `divide_by_zero_is_null` | `'null'::jsonb` where Python has `None` | `COMPILED_DIVERGES` | **`COMPILED_DIVERGES`** |
| I5 | `true_division` | `3.50000001` vs `3.5` (1e-8 out) | `COMPILED_DIVERGES` | **`COMPILED_DIVERGES`** |
| I6 | `modulo_pos` | `1.0000000001` vs `1.0` (1e-10 out) | `COMPILED_AGREES` | **`COMPILED_AGREES`** |

**I2 is the one that answers `FRAMING.md` §8 directly.** A case that fails to compile lands in the
denominator, not the numerator. The harness's own `write_report()` rendered:

```
| `DID_NOT_COMPILE` | 1 | 130 |
**Pass rate = 125/130 = 96.2%** — denominator is every fixture case, not every compiled case.
```

**I4 is the hardest case.** A compilation returning jsonb `'null'` where Python returns `None`
decodes to values that compare **equal** — `matches()` returns `True` — and the harness still emits
`COMPILED_DIVERGES`, via the `and not leak` conjunct at `:443`, with
`cause_shape: "SQL returned jsonb 'null' at top level, not SQL NULL (representation leak)"`. A
value-only oracle would have scored this a pass.

**I5/I6 bracket the epsilon.** *[corrected: the section originally called the 1e-9 absolute epsilon
"sharp in both directions". The two probes sit a full order of magnitude either side of it — this is
**bracketing**, not sharpness. Nothing here resolves behaviour between 1e-10 and 1e-8. The two
measurements themselves reproduced exactly: I5 `|3.50000001 - 3.5| = 9.99999993922529e-09 > 1e-09` →
diverges; I6 `mirrored_rule_agrees True` → agrees, which is correct, not lenient.]*

Emitted totals, exactly what the injections require:

```
"compiled_agrees": 125, "compiled_diverges": 3, "did_not_compile": 1, "sql_error": 1,
"pass_rate_denominator": 130, "pass_rate": 0.9615384615384616
```

and the whole reporting chain carried it — totals table, three loud per-outcome sections, per-case
rows, and group tallies (`### arithmetic — 11 cases (PASS 6 · FAIL 3 · GAP 1 · RAISE 1)`).

### 2.6 Three controls on the experiment itself

**The injection rig changes nothing when it does not fire:**

```
$ <venv-python> proto/conformance_injection_test.py --control
CONTROL RUN — compile_ast is the real one, no injection.
"compiled_agrees": 130, "compiled_diverges": 0, "did_not_compile": 0, "sql_error": 0, "pass_rate": 1.0
```

**The tracer measures the branches, rather than reporting zeros for lines it cannot see:**

```
$ <venv-python> .recheck/trace_outcome_lines.py --inject
line 377 hits=1  line 380 hits=1  line 396 hits=1  line 402 hits=1
line 443 hits=128  line 445 hits=125  line 451 hits=3  line 455 hits=3
```

125 + 3 = 128 = 130 minus the two cases that took branch A or B and `continue`d.

**SQL the harness cannot verify aborts the run rather than being scored:**

```
$ <venv-python> proto/conformance_injection_test.py --stray-percent
AssertionError raised, as required: stray literal '%' in generated SQL at offset 141
RESULT: the harness aborted. It cannot score a stray-% case at all.
```

*[corrected: the section called this "the fifth exit from the per-case loop … all five exits are now
exercised". The loop has more than five exits — `expr.parse` at `:359` and `expr.evaluate` at `:363`
are uncaught, and the mutation probe's `run_sql` at `:424-426` catches only `SqlRaised`. The re-check
traced `:405-408` executing under `--stray-percent` (hits 405/406/407/408 = 1), so the exit is real;
"the fifth and last" is what overstated. The conclusion is unaffected — every one of these exits
aborts the run, and none can silently score.]*

### 2.7 The oracle

`conformance.py:98-104` `matches()` mirrors `GIMS-Project/tests/test_dashboard_expr.py:20-25`
`_matches()`, including the bool-first branch with `type(actual) is type(expected)` and
`math.isclose(..., rel_tol=0, abs_tol=_EPS)`.

*[corrected: the section claimed the two spans are "character-for-character" identical. They are not
— `diff` over the cited spans returns a real hunk (6 lines vs 7, `def _matches` vs `def matches`, an
added docstring line). The **five logic lines are byte-identical**, which is what the claim needs and
what actually matters. The stronger wording does not survive.]*

The re-check's own best attack on this — that the mirrored rule's known nested-bool/int hole
(`matches([True],[1])` is `True`) would let a wrong compilation score as a pass — **fails on the
evidence**: the fixture has 0 list and 0 dict cases (46 float / 35 bool / 22 str / 19 None / 8 int),
and `strict_deep_equal` is `True` for all 130. That is also why lines `447-448` are dead.

---

## 3. Item 2 — the reconstructed evidence trail

**Section file:** `spikes/T-1/.recheck/trail.md` (43 KB).
**Adversarial re-check verdict: OVERSTATED.** Re-check file: `spikes/T-1/.recheck/adversarial-recheck.md`.

The re-check's finding, which is what this section reports: **the trail's evidence layer reproduces
exactly — every number it re-derived from a retained artifact came back to the digit, including the
two it offered as independent work — but its framing layer overstates in two places and misreports
three command outputs.** Its single headline *weakening* finding is materially overstated. The
corrected headline is therefore:

> The refusal registers all reconcile against raw artifacts. The trail has two real holes the
> document does not fully disclose — a closure seat that died mid-pass leaving 6 verification
> corrections permanently unapplied (2 of them material and still wrong in `FINDINGS.md` today), and
> three `probes.json` blocks with no surviving producer, one of which carries the `+463%` figure the
> document cites 11 times. The `+463%` exposure is real, **but `FINDINGS.md` already discloses it**;
> what is new is only the strike arithmetic. The 130-case jsonpath claim, previously untested,
> reproduces exactly.

### 3.1 What survives — re-derived, twice

Every item below was re-derived by the trail pass from raw artifacts and then **re-run independently
by the adversarial seat**, which reports each as exact.

| claim | how it was checked | result |
| --- | --- | --- |
| `FINDINGS.md` reassembles byte-for-byte from the 11 `.parts/` files | `cat` in documented order, `diff` | exact (delta is separators + blanks only) |
| B2÷A = **3.79×–7.15×** — the performance headline | recomputed from `analysis/measurements.json` | 4.152 / 3.892 / 3.794 / 4.363 / 6.713 / 7.152 |
| **68/130** subset coverage | re-tallied all 130 records in `analysis/subset-coverage.json` | panel 84 IN / A **68** / B 62 / C 56; panel-IN→A-OUT = 16 `{% 7, round 5, floor 2, ceil 2}` |
| the 130-case strict-jsonpath table (`FINDINGS.md:1879-1901`) | reconstructed instrument, run against `autosql_spike` | **exact, cell for cell** — 114/0, 10/10/9/1, 6/6/2/4, 130/16/11/5, including case 33 `$.x == null` → `expr` True, strict raw NULL, **DIVERGES** |
| all five consistency-repair refusals reconcile | re-derived each against the raw artifact it cites | all five hold (SQLite census, `xa` A.5(i) witness count, `grep` over `idxshape_*.sql`, `worker.started` count) |
| item 12 — the prescribed "+5 `Repo`" invents a collection | own `mode=ro&immutable=1` census of the three `objects.db` | guts `{Vector 6835, LedgerRecord 5186, WorkOrder 83, Repo 5}`; guts-ledger `{LedgerRecord 17430, WorkOrder 207}` — **no `Repo` row** |
| `xc`'s punch refusal closed itself one round later | read `xc:237` / `FINDINGS.md:3876` / `:5378` | 15 detectable + 19 undetectable = 34 ids; 15 + 18 = 33 classes |
| the corpus is gone; `xpr` survives | read-only psycopg2 to `autosql_spike` | `pg_tables` outside system schemas = **0 rows**; `xpr` = 21 functions |
| nothing in the spike is under git | `git ls-files spikes/T-1/` | **1 file: `FRAMING.md`** — no baseline exists to diff against, and that is broader than the trail claimed |

One incidental finding cuts *for* the document: verification correction **5**, which the trail lists
as unapplied, is **itself wrong** — `expr.py`'s unary `+` really is at `:180-182` as `FINDINGS.md:947`
publishes; the verifier's `179-181` is off by one.

### 3.2 Hole 1 — a closure seat died, and nothing says so

`.parts/closure-reports.md:13` reads: `(agent died: API connection lost mid-response; file
untouched)`. A `grep -in 'agent died\|API connection\|connection lost\|died'` across `FINDINGS.md`
and every `.parts/` file returns **that one line only**. `FINDINGS.md` does not disclose it.

The consequence: **all 6 of `f2`'s verification corrections were never applied.** `f2`'s own
compliance block (`FINDINGS.md:1330`) states "The single §2.4 edit above is the only change made to
this file after it was first written", and `verifications.json['Finding 2']['corrections']` has
length 6.

**Two of the six are material and are still wrong in `FINDINGS.md` today.** Both were reproduced
independently by both seats:

- **C1 — recursion limits.** Re-bisected read-only against real `expr.parse` + `proto/compile.py`:
  first `RecursionError` at `or` n=333 (1,661 chars), `and` n=333 (1,993), `not` n=332 (1,329).
  `FINDINGS.md:1124` still publishes **400 / 334 / 499** at ~1,996 chars under the header "first
  failing size" (`:1121`) — those are the *largest sizes under `MAX_SOURCE_LEN`*, which is a
  different quantity.
- **C6 — parse depth.** `GIMS-Project/core/dashboard/expr.py:186-187` increments *then* tests
  `> MAX_DEPTH` (= 64 at `:40`), so maximum parsing depth is **63**. `FINDINGS.md:1143-1144` still says
  "The parser permits depth 64".

A third (**C2**) is confirmed against raw JSON: `proto/results.json` records **8** out-of-fixture
probes, `Counter({'DIVERGES': 4, 'agrees': 3, 'SQL_ERROR (totality violation)': 1})`, while
`FINDINGS.md:925`'s headline row still reads an unscoped **403 / 403**.

Neither seat re-ran the remaining three (the 33-file `gims-ledger` sweep, and two GIMS docstring
cites) — see §5.

### 3.3 Hole 2 — three orphan blocks, and the `+463%` figure

Three blocks in `probes.json` have no surviving producer. `proto/probe_extra.py` is the only retained
writer of that file and it writes in **truncating** mode (`out = {}` at `:20`; `json.dump(..., "w")`
at `:100`), setting only `payload` and `poison` — so re-running it would destroy all three blocks.
The measured corpus is gone (every `measure_instances_*` and `idxprobe` table dropped).

Of the three:

- **Block 1 (`xpr_decomposition_100k_ms`)** is harmless — it is cited only to say the experiment was
  *discarded, not reported* and its inference is *unconfirmed*.
- **Block 3 (`recheck`)** *strengthens* the NO-GO when demoted: dropping it raises the B2/A floor from
  2.55× to **3.79×**, which is worse for the compiled arm. `FINDINGS.md:5192` books this as "the
  performance leg — **firmer**".
- **Block 2 (`poison_syncscan`)** is the load-bearing one. The **`+463%`** run-time-refusal figure —
  cited **11 times** (`grep -c '463%' FINDINGS.md` = 11, re-verified here) — rests entirely on
  `poison_syncscan.off.median_ms = 6916.85`, which cannot be reproduced. Struck, the identical claim
  prices at **+2.2%** from the retained fallback numbers: `(33.3 + 1493.92) / 1493.92`.

**This is the item most worth the owner's attention in §3, and it is an argument for the other option.**
It is treated honestly in §3.4 and §5.

### 3.4 Where the trail overstated — the re-check's corrections

These are the re-check's verdicts, and they replace the trail's own claims.

**M1 — "an argument the document never concedes" is FALSE.** The trail's single headline weakening
finding was that the `+2.2%` fallback is an objection *"the document does not concede."* It does,
verbatim, at `FINDINGS.md:2875` (`f4` §4.10 item 10):

> "`probes.json → poison` **still carries the superseded values** `time_to_raise_ms: 33.3`,
> `total_fallback_ms: 1527.22`, `overhead_pct: 2.2`. A reader who opens the raw file first gets
> **+2.2%, not +463%**" — flagged Material? **"Yes, for anyone re-deriving."**

The document also already labels `+463%` *"a construction, not one observed event"* (`:2838`) and
states that every claim resting on the orphan blocks *"inherits that"* un-auditability (`:2894`). The
trail **quotes `:2875` in its own §2.2** and then writes the opposite two sections later. What is
genuinely new is only the **strike arithmetic** — that removing the orphan block leaves `+2.2%` as
the sole retained price. That is a real contribution; *"never concedes"* is not supportable. Since
this was the trail's load-bearing weakening finding, the overstatement is load-bearing too.

**M2 — the seat-count contradicts the trail's own table.** The trail published *"across all six
passes there were 9 + 1 + 9 + 3 + 7 + 1 = 30 seat-instances; reports survive on disk for 9 of them."*
That sum covers passes 3, 3b, 4, 5 and 6 only. The trail's own pass table lists pass 1 = `f1–f5 (+f0)`
= 6 seats and pass 2 = verifier + critic + 3 panel seats = 5 seats — **eleven seats it counts as
passes and then omits from a total labelled "across all six passes"** (`panel.json` is a 3-element
list, confirming the 5). Corrected: **~41 seat-instances, with records surviving for all eleven
omitted ones** (the part files themselves, `verifications.json`, `critic.md`, `panel.json`). The
published fraction — 9 of 30, 30% — **understates surviving audit coverage by roughly half**
(≈20 of 41, ≈49%), in the direction that makes the trail gap look larger than it is.

**M3 — three reported command outputs do not reproduce.** In a document whose stated method is
*"anything I ran is named with its command and its actual output"*, this matters on its own terms:

1. `grep -rn 'probes.json' proto/ analysis/` → **4 hits**, not "one hit": `proto/probe_extra.py:99`
   **plus** `analysis/measurement.md:306, 468, 810`.
2. `grep -rn synchronize_seqscans proto/ analysis/` → **3 hits**, not zero:
   `analysis/measurement.md:639, 643, 644`. Those lines are a **retained prose account of the syncscan
   experiment's method** — *"`synchronize_seqscans = on` (the default) | 40.8 ms | the scan happened to
   start 7 pages before the poison row"*. `trail.md:244` correctly qualifies this as "zero hits in any
   retained **script**"; the structured result drops the qualifier. So Block 2 is script-less, **not
   method-less**.
3. `grep -i refus .parts/f5.md` → **12 hits**, not the reported 8. The substantive conclusion (none
   concerns refusing a punch repair) holds; all twelve were read.

**Cosmetic, listed for completeness:** four cite drifts (`out = {}` is `probe_extra.py:20` not `:19`;
the reassembly delta is 9 `---` lines + 19 blanks, not "ten separators";
`xpr_decomposition_100k_ms` is named twice in `FINDINGS.md`, not once); and `trail.md:335`'s
"210-fold reduction in the priced worst case" is the ratio of overhead **percentages** — the priced
worst case in milliseconds falls 8,410.77 → 1,527.22, a **5.5×** reduction, which a gate reader could
take the wrong way.

*Assembler's note, since I checked rather than copied:* the "Firmer: the performance leg" sentence
begins at `FINDINGS.md:4683` — the trail cited `:4684` and the re-check corrected it to `:4682`.
Both are off by one, in opposite directions. `:5192` and `:5193` are exact. Nothing turns on this; it
is recorded because this document should not launder a cite it did not verify.

### 3.5 The jsonpath claim — reproduced, with one caveat that stands

`FINDINGS.md:2116` asserted the 130-case strict-jsonpath result was re-derivable. It had **never been
tested**. It now has been, twice, and reproduces exactly (§3.1).

The caveat the trail added, which the re-check independently confirmed and called "correct and
important": **neither named instrument contains an AST→jsonpath translator**, which is where the
semantic risk lives. `idxshape_jsonpath_agree.py`'s jsonpath strings are hand-written literals in a
hard-coded 11-case list that never reads the fixture, and it renders `$.score > 90` in the **filter**
form `$."score" ? (@ > 90)` while the published table and the reconstruction both use the **direct**
form `$."n" < 7`. The translation convention is supplied by the reconstruction, not by the spike. The
best available substitute is now committed as `proto/idxshape_jsonpath_130.py`.

---

## 4. What this changes about the ruling

**Net: the ruling is UNCHANGED.** One stated leg is weakened, one is very slightly strengthened, and
the three facts the decision turns on are untouched. Detail follows, because "unchanged" is doing
different work in each half.

### 4.1 Item 1 weakens one stated NO-GO leg

`FINDINGS.md:5193` books "the fixture-adequacy leg" as **firmer**, on this explicit ground:

> "130/130 was scored by a harness whose 'compiled and diverges' and 'did not compile' branches have
> **never been emitted, only inferred**."

**That ground is now gone.** They have been emitted, correctly, through the real per-case loop. The
same residual is claimed again at `FINDINGS.md:4683` (`f5` §5.5's summary: §5.2 "gains the residual
that `conformance.py`'s outcome-assignment branches were never exercised"). That residual is spent.

The leg does not collapse — it falls back on §5.2/§5.7's own argument, that the fixture covers **68 of
130 (52.3%)** of the corrected CONDITIONAL-GO subset and was never built to be an acceptance test for
a third runtime. That was always the stronger half, and Item 2 re-derived the 68/130 exactly.

**The correct reading is that 130/130 got *stronger*, not that the NO-GO got weaker.** 130/130 was
never what the NO-GO rested on — it is the pro-GO fact the NO-GO had to account for, and it now
stands as a credible fact rather than a possibly-hollow one. The load-bearing legs (performance
3.79×–7.15×; 18 undetectable classes of 33; the `FRAMING` §5 raise→value breach) are wholly
unaffected. The outcome that **would** have changed the ruling — the rig scoring `DID_NOT_COMPILE` as
a pass, voiding every downstream conformance number — is precisely the one this pass did not get.

### 4.2 Item 2 leaves the ruling standing and moves the record both ways

**Toward the ruling.** Every measurement re-derived from a retained artifact came back exact: the
3.79×–7.15× headline, the 68/130 subset coverage, all 16 jsonpath rows including the disqualifying
case-33 silent row drop. Demoting the un-audited `recheck` block *raises* the B2/A floor from 2.55×
to 3.79× — worse for the compiled arm.

**Away from it, in one quantified place.** `+463%`, cited 11 times, rests on an unreproducible
6,916.85 ms and prices at `+2.2%` without it. That is a legitimate CONDITIONAL-GO argument and the owner
should see it. Two things bound it, both verified: `FINDINGS.md:2875` **already concedes** the `+2.2%`
fallback and flags it Material (§3.4 M1), and `f4` §4.9 already states that the figure never bounded
the 18 undetectable-in-principle classes the verdict actually rests on. So it sharpens a disclosed
weakness rather than exposing an undisclosed one.

**The real damage is to the document's account of its own process, not to its findings.** Two things
are true of `FINDINGS.md` today and were not disclosed by it: a closure seat died mid-pass, and 6
verification corrections were consequently never applied, 2 of them still wrong in the published text
(§3.2). Neither touches any of the three decision facts — they are accuracy defects in `f2`'s
reporting of `expr.py`'s recursion and depth limits — but a reader auditing the document is entitled
to know them.

### 4.3 What the ruling now rests on, restated honestly

Unchanged and load-bearing:

1. **`resolve()` at `GIMS-Project/api/dashboard/sources.py:357` returns `{records, count, truncated}`**
   — no channel for a fallback to be reported, so `FRAMING` §5's non-negotiable cannot be met without
   a GIMS contract change plus a frontend change. Untouched by either item.
2. **Performance is negative** — 3.79×–7.15×, no crossover, gap widening with N. **Re-derived exactly
   in this pass.** Q11's answer ("index work stays off") removes the only route by which the compiled
   path could have got faster, which makes this leg harder, not softer.
3. **18 of 33 divergence classes are undetectable in principle**, and the CONDITIONAL-GO subset covers
   52.3% of the fixture with a non-empty residual. **Re-derived exactly in this pass.**

Changed by this pass:

- The fixture-adequacy leg loses its "never emitted, only inferred" residual and keeps its 68/130
  argument.
- `+463%` should be read as `+2.2%` retained plus an unauditable construction, not as a measured
  figure — and `FINDINGS.md` should say so at all 11 citation sites, not only at `:2875`.

**Nothing in this pass was edited into `FINDINGS.md`.** The corrections above are recorded here and
are `sp-synth` work if the owner wants them carried into the document.

---

## 5. What is still not established

Ordered by how much it matters to the decision. Each item names what would settle it.

### 5.1 The other instruments were not tested — and one of them is the important one

Q4 asked whether "the test rig" can report a failure. **Only `conformance.py` was tested.**
`FINDINGS.md:4869-4870` sits `conformance.py`'s §5.9(6) residual in a list that also names
**`differ.py`** and **`bench.py`** as having **no negative controls** (`critic` §7), with this
sentence attached, verbatim:

> "— the instrument that produced every decision-relevant divergence has not itself been shown able
> to fail"

That residual stands entirely unchanged.

*What would settle it:* the same treatment — inject a known-wrong input and assert the emitted
classification. `proto/conformance_injection_test.py` is a working template. This is a small, bounded
run, and it is arguably a better use of a day than anything else on this list.

### 5.2 Whether `compile.py` itself is correct

The injection replaces `compile_ast`'s **output**. It proves the harness classifies a wrong
compilation correctly — it tests the judge, not the defendant. A compiler producing SQL that agrees
with Python *for the wrong reason* would still score `COMPILED_AGREES`, correctly, under this rig.

*What would settle it:* out of this pass's scope. It is the divergence register (`xc` C.8) and the
out-of-fixture probes, which already found real defects (`float8_overflow_raises`; the `xpr.f8` guard
being ~12 orders of magnitude too tight).

### 5.3 Whether 130/130 means the compiler is *adequate*

A sound rig scoring an inadequate fixture still produces an inadequate result. §5.7 measures fixture
coverage of the corrected subset at **68 of 130 (52.3%)**, and the fixture was never built to be an
acceptance test for a third runtime. This experiment does not bear on that charge at all.

*What would settle it:* **E1**, the subset acceptance battery — zero across the three silent classes
and zero raise→value under a generator reaching the eight raise sites. Q2's answer ("not good enough
— build a real one") already funds exactly this.

### 5.4 Whether the 130 still agree at other float-digit settings

`conformance.py:341` hard-codes `SET extra_float_digits = 1` and every run in this pass inherited that
pin. `FINDINGS.md` §5.11 names it as an open gap with 68 of 130 cases exposed.

*What would settle it:* re-running with the GUC varied — a one-line change to `:341` plus one run per
setting. **Q10 already funds this** ("make the correctness run test all three settings"), so it is
scheduled work, not a gap in the record.

### 5.5 That a real divergence would be reported with a *named* cause

`CAUSES` is an empty dict (`conformance.py:267`), so all three injected divergences reported
`cause: 'UNCLASSIFIED'`. That is by design (`:265-267`: a case absent from `CAUSES` is "reported as
unclassified rather than guessed at"), and the mechanical `cause_shape` **is** populated and accurate.
But `OUTCOME_DEFINITIONS['COMPILED_DIVERGES']` at `:224-229` promises "Cause is named per case", and
nothing names it automatically. This is a wording overstatement in the spike's own document, not a
machinery defect. It was not changed.

*What would settle it:* nothing — it is recorded as an observation. If it matters, the fix is one
sentence.

### 5.6 That the harness run reproduced is the *same* run the spike made

The `autosql_spike` database did not exist at the start of this pass and had to be rebuilt from
`proto/runtime.sql`. Byte-identical reproduction of 130 cases, 23 controls, 8 probes and the full
rendered report is strong evidence the reconstruction is faithful — but it is reproduction, not the
original run.

*What would settle it:* nothing available. The original database is gone; byte-identical reproduction
is the strongest evidence obtainable.

### 5.7 What five punch-round seats and four adversarial reads actually said

No artifact exists for them. For `xb` and `xd` the word "refuse" does not occur in the file at all, so
the closure log's stated fallback method ("read the refusal off the seat's own in-place text") had
**zero recall** on those seats and could not have recovered one even in principle. Likewise, no
artifact was written for the three punch-round adversarial lens reads or the fourth final-check read;
*"21 items, all credibility or minor, none decision-blocking"* and *"gate-ready, 5 defects"* are
**dispatch summaries written by the seat being summarised**.

This matters because the handoff's confidence paragraph — "24 defects (4 decision-blocking) → 21 (0)
→ 5 (0) → the final read" — rests on precisely these unretained summaries. It is the largest single
hole in the trail, and it sits under the claim doing the most work.

*What would settle it:* only the seats' transcripts, if the driving session retained them.
Unrecoverable from the repo.

### 5.8 Whether the orphan `probes.json` blocks came from the code their prose describes

No producer script survives; every `measure_instances_*` table has been dropped. A retained *prose*
account of the syncscan method does survive at `analysis/measurement.md:639-644` (§3.4 M3), so the
block is script-less rather than method-less.

*What would settle it:* only a fresh experiment — `gen_data.py` is seeded (`SEED=1729`) so the corpus
rebuilds identically, but a new syncscan-controlled probe yields **new** numbers on a different host
at a different load. That is corroboration, not re-derivation.

### 5.9 Loose ends neither seat closed

- **`f2` corrections 3, 4 and 5.** Corrections 1, 2 and 6 were reproduced; the 33-file `gims-ledger`
  sweep (4) and the two GIMS docstring cites (3, 5) were not re-run. Note correction **5 is itself
  wrong** (§3.1). *Settled by:* re-running the sweep; opening `GIMS-Project/core/deep_search.py:168`
  and `:381-391`, and `expr.py:175-213`.
- **`xb`'s numerator 4,166.** Only the denominators were re-censused, and they have moved
  (guts-ledger 17,637 vs published 17,345; guts 12,109 vs 12,095). That movement **confirms** `xb`'s
  own caveat — a snapshot pinned to a checkpoint, not a standing property. *Settled by:* re-running
  `xb`'s collision probe read-only with `_norm_key` as oracle.
- **Whether the original 130-case jsonpath script derived its result the way the reconstruction does.**
  It was never retained. Reproducing a number does not certify the original run. *Settled by:*
  nothing; `proto/idxshape_jsonpath_130.py` is the best available substitute and is now committed.

### 5.10 This document's own trail is asymmetric — stated, since Q5 is about exactly this

The adversarial re-check of Item 2 is on disk at `spikes/T-1/.recheck/adversarial-recheck.md`. **The
adversarial re-check of Item 1 is not.** Its verdict (STANDS, five cosmetic defects) reached this
document as a structured result only; no artifact was written for it. Item 1's own working
(`.recheck/harness.md`) and every command output it cites *are* on disk, and the load-bearing hashes
were re-verified while assembling this document — but the re-check that says Item 1 stands has
exactly the property Item 2 was sent to investigate.

*What would settle it:* re-running the Item 1 re-check with its report written to
`.recheck/adversarial-harness.md`. Cheap, and it closes the one place where this pass repeats the
failure it was auditing.

---

## 6. Files changed by this pass

`FRAMING.md` §3's no-edit rule was waived by the owner in writing on 2026-08-21 (Q7: *"Let them edit the
existing code in place"*). Everything below is what that waiver was used for.

### New instruments, in `proto/`

| path | what it is |
| --- | --- |
| `spikes/T-1/proto/conformance_injection_test.py` | The Item 1 experiment. Drives the real per-case loop with six injected wrong compilations; `--control` and `--stray-percent` modes |
| `spikes/T-1/proto/idxshape_jsonpath_130.py` | The Item 2 reconstruction of the 130-case strict-jsonpath result. The substitute for the never-retained scratchpad script |

### New records, in `.recheck/`

`harness.md` (Item 1 working) · `trail.md` (Item 2 working) · `adversarial-recheck.md` (the
adversarial re-check of `trail.md`) · `trace_outcome_lines.py` · `injection-run.txt` ·
`injection-control.txt` · `trace-normal.txt` · `trace-injected.txt` ·
`injected/CONFORMANCE.injected.md` · `injected/results.injected.json` ·
`orig/CONFORMANCE.md.orig` · `orig/results.json.orig`

### Regenerated, content unchanged

| path | note |
| --- | --- |
| `spikes/T-1/proto/results.json` | new mtime, **sha256 unchanged** `002cda09…` |
| `spikes/T-1/proto/CONFORMANCE.md` | new mtime, **sha256 unchanged** `72db7f82…` |

The `autosql_spike` Postgres database was rebuilt from `proto/runtime.sql` (it had been dropped
before this pass began) and then written to by the harness runs. `glp_strong` untouched.

### Deliberately NOT changed

- **`spikes/T-1/FINDINGS.md` is untouched.** Every correction this pass found — the spent
  "never emitted" residual at `:5193`, `f2`'s two live errors at `:1124` and `:1143-1144`, the unscoped
  403/403 at `:925`, the undisclosed dead seat, the `+463%` framing — is recorded **here** and is
  `sp-synth` work if the owner wants it carried in.
- **`proto/compile.py`, `proto/conformance.py`, `proto/runtime.sql`** — sha256 verified unchanged
  (`b71b1538…`, `fa545b0c…`, `32628b45…`); the first and third still match what `results.json`
  recorded as its inputs.
- **Both GIMS checkouts stayed read-only.** `find ../GIMS-Project -type f -newermt "2026-08-21 00:00"`
  returns nothing; `git status --porcelain` shows only pre-2026-08-21 modifications;
  `gims-ledger` was opened read-only (`mode=ro&immutable=1`) and never written.
- **`tracker.mjs`, `.autodev/tickets/`, `.autodev/events.jsonl`** — not touched by this pass. The
  driving session is the only writer of ticket state.

### One thing worth knowing about all of the above

`git ls-files spikes/T-1/` returns **one file: `FRAMING.md`.** Nothing else in the spike — not
`FINDINGS.md`, not `proto/`, not `analysis/`, not any of this pass's output — is under version
control. There is no baseline to diff against and no history to audit. Given that Q5 is a question
about the durability of this spike's evidence, that is the single cheapest thing to fix.
