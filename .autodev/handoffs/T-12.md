# T-12 — README and AGPL-3.0 license for autoSQL
Intake 2026-09-04 (agent:pm): a feature, small. Evan's ask verbatim: "Give autosql a readme and an agpl license." Pre-flight for going public found no secrets. Lane: feature, lean. Spec is complete as given (two files); refine confirmed it rather than rewriting. Worktree `../autoSQL-T-12`, branch `t-12-readme-license`. Not in scope: visibility flip, description, stray files.

## Build (2026-09-04)

**Produced:** `README.md` (139 lines) and `LICENSE` (GNU AGPL-3.0, 34,524 bytes, verbatim from
`gh api /licenses/agpl-3.0`, unedited). Both on branch `t-12-readme-license` in the worktree
`../autoSQL-T-12`, one commit, `f25f4014cfa7df568d995dfbe4f97ad4e7c6d313`. **Not pushed.** Nothing
in the main checkout was changed except this file and the evidence file. No existing file was
deleted, moved or edited; repo visibility and description untouched.

The README covers, in order: what autoSQL is; why it exists (the silent-wrong-number failure mode,
plus one sentence on GIMS linking `gims-oss`); the tree (`compiler/`, `runtime/`, `demo/`,
`spikes/`, `kb/`, `design/`, `ops/`, and a line naming `.autodev/` as the AutoDev ledger); how to
run the demo with the real commands from `start.sh` / `run-demo`; what is proven and what is not;
status; license. Every figure is cited inline to the KB page it came from — the full map is in
`.autodev/evidence/T-12/build.md`. Banned-word grep is empty.

**Unsure about / worth a second pair of eyes:**

1. **Step 11, and I did not write what the brief asked for.** The brief said to describe step 11 as
   a deliberate, flagged disagreement between the SQL and Python panes. It is not one any more —
   T-8 reconciled it and both panes now report `123` (`kb/CURRENT-WORK.md` "must not miss" #2,
   `demo/WALKTHROUGH.md`, `demo/expected-answers.json` `steps[10]`). I wrote the current truth
   instead, and added one sentence saying `demo/README.md` is stale on this point. If the intent
   really was to ship the old description, that is a one-line revert — but it would be wrong the
   first time anyone runs `./start.sh`.
2. **`demo/README.md` is stale and I did not fix it.** Its step-11 paragraph still describes the
   disagreement as live. Out of this ticket's two paths, so left alone. Worth its own small ticket
   before the repo goes public.
3. **GIMS in one sentence.** No KB page defines GIMS outright, so the sentence ("the
   information-management system this plugs into — nouns, verbs, dashboards, and the small
   per-record expression language those widgets are built from") is assembled from
   `kb/wiki/autosql-architecture.md` and `.autodev/notes/QUESTIONS-FOR-EVAN.md`. Evan may want to word it himself.
4. **The slowdown figure** is written as **3.79×–7.15×** (the KB's and Evan's own wording), not the
   brief's rounded 3.8×–7.2×.
5. **Pre-existing dirty file.** `.autodev/conformance-history.jsonl` was already modified in the
   worktree before this stage began. Not staged, not committed, not touched.

Nothing was run against any database, and `tracker.mjs`, `.autodev/events.jsonl` and
`.autodev/tickets/` were not touched.

Touched-files: README.md, LICENSE
