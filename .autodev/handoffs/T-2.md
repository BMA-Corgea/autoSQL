# T-2 handoff — stopped 2026-08-23 05:40 UTC, deliberately

**Read this, then `kb/CURRENT-WORK.md`. Nothing else is needed to resume.**

## Where it is

**`build`, attempt 4, BLOCKED.** The block reason on the ticket says the same thing this does.
Everything is committed and pushed; the working tree is clean at **`28a9c9a`** on `main`.

**Suite: 1144 passed, 2 failed.** Both failures are the same finding, below. Nothing else is red.

## The one thing that needs Evan

**Adopting T-3's corrected runtime deletes the demo's centrepiece disagreement.**

Verified on the running stack, not inferred:

```
xpr.f8(1e300) = 1e+300        # the OLD 297-digit guard returned NULL here
xpr.f8(1e400) RAISES XPR01    # named refusal, as designed
```

The demo's showcase state was a pick over `edge-01` where `l = [1e300, 1]`: SQL answered **1**,
Python answered **1e+300**, and that gap was the whole visual argument for the screen. It was
real — **but it was an artifact of the guard defect.** The 297-digit literal was nulling `1e300`,
so SQL's `max()` never saw it. T-3 fixed the guard to 309 digits; SQL now reads `1e300` correctly;
**the two engines agree and `differing_rows` is 0.**

So his q4 answer, applied faithfully, removes the thing the demo exists to show. He was told
adopting would change four criteria. **Nobody predicted it would empty the disagreement state.**

**His options — a product question, not a build one:**
1. Find a different in-subset disagreement the corrected runtime still produces.
2. Keep the pinned pre-fix runtime **for the demo only**, and adopt the fix everywhere else.
3. Accept a demo whose disagreement state is a **refusal** rather than a divergence.

Asked in the form: `https://claude.ai/code/artifact/79700309-4e45-45fa-9d4e-998a5f5c51fb`

## What is done, and what is half-done

| | state |
|---|---|
| **q3 — re-scope AC-35** | **DONE.** Scoped to the seven files the ticket vendors. Its two standing failures are gone; his uncommitted GIMS edits were never touched. |
| **q4 — adopt the corrected runtime** | **CODE DONE, DOCS HALF-DONE.** `demo/vendor/runtime.sql` is now the 472-line corrected version and the manifest matches. **B15, B24 and AC-13 carry their dated amendment notes; AC-22's is MISSING** — the worker was stopped mid-sentence on exactly that. |
| **q8 — the layout fix** | **NOT STARTED.** Move the differing column beside the coral marker. Required before acceptance per GA-8. |

## Then, in order

1. Settle the disagreement question above; unblock.
2. Finish AC-22's amendment note in `.autodev/specs/T-2.md` (same dated-note pattern as AC-17 and
   B15 — **leave the original signed wording intact**).
3. Fix whatever the settled answer implies for the two failing `test_ui.py` tests. **Do not make
   them pass by weakening them** — if the divergence genuinely no longer occurs, the test should
   assert what now happens, with a comment saying why it changed.
4. Do q8's layout fix.
5. Re-verify, then **accept** — the gate is `human` again (he softened it), so it can be cleared
   on-behalf against GA-8, which authorises acceptance **only after the layout change lands**.

## Standing constraints — unchanged

- **Never connect to port 55433** — Evan's live database.
- The **GIMS checkouts are read-only**; his uncommitted edits from 13 Aug stay untouched.
- The `design` gate is `human:strict` and stays that way — he asked for that one.
- Plan §8.2's mutation pass has still never run; 9 of 16 mutants never watched failing. Disclosed
  above every suite summary.

## The machine, as left

Demo stack **torn down** (container and volume removed). Nothing listening on 55440 or 8787.
The isolated worktree at `/home/corgea/autoSQL-T-2-build` still exists on branch `feat/T-2-demo`;
it is merged and can be removed with `git worktree remove` whenever convenient.

Touched-files: .autodev/specs/T-2.md, demo/vendor/runtime.sql, demo/manifest.json, demo/builder.py, demo/probes.py, demo/seed/*, demo/server/app.py, demo/tests/*, demo/EVIDENCE.md, demo/README.md, demo/VERIFY.md, demo/WALKTHROUGH.md, demo/expected-answers.json, demo/vendor/NOTICE.md
