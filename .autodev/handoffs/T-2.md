# T-2 handoff — 2026-09-01, at the accept gate

**Read this, then open the screen. Nothing else is needed.**

## Where it is

**`uat`, `awaiting_approval`. The `accept` gate is UNCLEARED and it is Evan's.**
Everything that was outstanding is done. Work is on `main`, tree clean.

**The stack is UP:** `http://127.0.0.1:8787` (app) and `127.0.0.1:55440` (its own Postgres).
`./run-demo down` stops it, `./run-demo up` brings it back.

## Why the gate was NOT cleared on his behalf

GA-8 does authorise acceptance — *"this go-ahead authorises acceptance only AFTER the layout change
lands and re-verifies"* — and both conditions are now met. **It was still left for him, deliberately.**

This ticket's own purpose is that **Evan sees the SQL UI over fake data before autoSQL goes into
GIMS**. Accepting it on his behalf would defeat the thing the ticket exists to do, and the gate's own
question is *"would you show it to someone?"* — which is not a question this seat can answer for him.

Everything else in the loop was driven on-behalf under GA-10/GA-11. This one stop is his.

**To accept:** `tracker.mjs approve T-2 accept --by human:evan --i-am-human`

## What changed this session

| | |
|---|---|
| **The two standing failures** | **FIXED.** They were telling the truth. |
| **q8 — the layout fix** | **DONE**, and verified live. |
| **AC-22's amendment note** | **FINISHED.** |
| **Suite** | **1152 passed, 0 failed** (was 1144/2). |

**The two failures were real, not flaky.** `ACCEPTED_PICKS["the disagreement"]` was moved to `$.m`
on 2026-08-23; **`SEVEN_STATES["disagree"]`, a second definition in the same file, was missed** and
stayed on `$.l`. `$.l = [1e300, 1]` *agrees* now that the corrected guard reads `1e300` properly — so
that state had genuinely stopped being a disagreement and the tests said so for nine days. Both now
point at `$.m` (`["１２３", 1]`), where Python reads **123** and the ASCII gate returns NULL so SQL
answers **1**. Real, in-subset, and not an artifact of a defect anyone intends to fix.

**q8, as ruled** (*"move the differing column beside the marker"*): the grid is
`[SQL | coral spine | Python]`, so the order is **mirrored about the spine**. Computed on the
**server** and published as `column_order` (a permutation of column indices), which is why its six
tests are real end-to-end assertions rather than a grep over the bundle. `columns` and `kinds` are
untouched, so every criterion asserting on them still holds. **An agreeing pick is not reordered at
all.** Verified live:

```
SQL   order : collection · key · data · biggest      at the marker: 1
Python order: biggest · collection · key · data      at the marker: 123
```

## What to look at, in order

1. **`http://127.0.0.1:8787`** — the screen. The tab strip has the seven states.
2. **The `disagree` tab.** One row is marked `≠`. The `biggest` column now sits **immediately either
   side of the marker** — `1` on the left, `123` on the right — instead of the reader scrolling
   sideways for values the banner already named. That is q8.
3. **The other six tabs**, if you want them: three agree, three refuse at the layer they should.
   All seven were exercised live — `demo/VERIFY-2026-09-01.txt`.

## The one thing to know before accepting

**This demo's centrepiece disagreement will stop working when T-8 lands, and that is not a bug.**

T-6 adopted **variant C**, which makes SQL and Python agree on `１２３` as well. When T-8 puts that
runtime into `demo/vendor/`, `$.m` stops diverging exactly the way `$.l` did.

**On present evidence there is no in-subset value disagreement left after T-8** — that is what T-6
passing *means*. So step 11's claim has to change from *"the two engines disagree"* to *"the tool
refuses rather than guessing"* (the magnitude refusal at `edge-03`, already demonstrated at step 13).

**That changes what the demo argues, so it is Evan's call, not T-8's.** It is written down against
AC-22 in `.autodev/specs/T-2.md`, in `demo/EVIDENCE.md`, and in `kb/CURRENT-WORK.md`, so T-8 cannot
make it by accident.

## Standing disclosures, unchanged

- **Plan §8.2's mutation pass has still never run.** 9 of 16 mutants have never been watched
  failing. The runner discloses it above every suite summary; nothing this session changed that.
- **Never port 55433.** Not touched.
- The `design` gate stays `human:strict`.
- The stale worktree at `/home/corgea/autoSQL-T-2-build` (branch `feat/T-2-demo`) is merged and can
  be removed with `git worktree remove` whenever convenient.

## Reproducing any of it

```
./run-demo up                                  # stack
./run-demo test                                # 1152 passed, + the B10 checksum guard
demo/.venv/bin/python ops/verify-seven-states.py   # the seven states, live over HTTP
```
