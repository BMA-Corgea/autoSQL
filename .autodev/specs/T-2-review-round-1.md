# T-2 · auto-review · report (4 lenses, every finding adversarially verified)

**21 agents: 4 reviewers, then one refuting verifier per finding.** Each verifier's job was to
prove the finding WRONG, defaulting to `real: false` when uncertain — so a plausible-but-wrong
finding does not reach the owner. **16 confirmed, 1 refuted.**

## Confirmed findings

### [HIGH] §7.4 comparator misses Postgres's top-level empty-array exception — a reachable pick makes the panes disagree with the SQL side RIGHT
`demo/pyrunner/order.py`:93 · lens: sql-arithmetic

**What is wrong.** Postgres's documented jsonb btree order has an exception the spec's §7.4(1b) table omits and order.py therefore does not implement: an EMPTY TOP-LEVEL ARRAY sorts below everything, including null. Measured on the demo's own database (PG 16.14, C collation): SELECT '[]'::jsonb < 'null'::jsonb returns true, and an ORDER BY over a crafted 27-value jsonb set puts [] first while compare_jsonb ranks [] in the array band (above booleans/numbers/strings/nulls). The exception is top-level only — nested [] inside containers follows the normal table, which compare_jsonb's recursion gets right — so the defect is exactly at sort_key's top-level value comparison. demo/tests/test_order.py:220 (test_the_type_ladder) uses [] as its array exemplar, so the suite literally pins the divergent value: the test asserts False < [] which real Postgres contradicts at top level. The spec text itself says the table was written out because 'a mismatch produces a disagreement that looks like a finding' — this is that mismatch.

**Concrete failure.** Reproduced end-to-end through the running demo: pick {source: noun:EdgeCase, computed: [{name: mix, expr: coalesce($.arr, $.tags, $.l, 0)}], sort: {field: mix, dir: asc}} — every construct in-subset, seeded rows only. SQL pane (real Postgres) orders edge-09 (mix = []) FIRST; Python pane orders it eighth, after all numbers; verdict 'disagree', 8 differing rows. The screen shows the red disagreement banner — which the demo's whole framing teaches means 'the SQL generator produced a wrong answer' — when the generated SQL is correct and the independent control is wrong. For a demo whose one job is making disagreement mean something, a false alarm reachable from the picking UI is the §5 control crying wolf. No seeded PATH sort can hit it (no seeded column mixes a top-level [] with another present value), so the walkthrough and ACs pass; only an op-2 computed column that passes a container through (coalesce suffices) exposes it.

**Why it survived refutation.** Every element of the claim survived verification against the real code and the running stack. (1) The Postgres behavior is real and measured on the demo's own database (PG 16.14, C collation, port 55440): '[]'::jsonb < 'null'::jsonb returns true, '[]' < 'false' and '[]' < '""' return true, '{}' < 'null' returns false (no object analogue), and '[[]]' < '[null]' returns false — confirming the exception is top-level-only, exactly matching PostgreSQL's documented jsonb btree quirk. (2) demo/pyrunner/order.py implements spec §7.4(1b)'s table with no top-level empty-array special case: _type_rank (line 93) ranks [] in the array band above null/string/number/boolean, so compare_jsonb contradicts Postgres on every top-level ([], scalar) pair. The spec table itself omits the exception; the code faithfully implements an incomplete table. (3) demo/tests/test_order.py::test_the_type_ladder (~line 21

### [HIGH] Every aggregated value in all 568 tests is a small integer, so a float-contaminated Python aggregate passes the whole suite — the plan-promised decimal-aggregate case (M9 row, B7) was never added
`demo/pyrunner/evaluate.py`:395 · lens: test-integrity

**What is wrong.** The shipped code is correct, but the suite cannot see this class of defect. Every aggregate/window pick in the entire suite reads $.payload.load (integers 0-100) or an integer fixture — grep of demo/tests for fn sum/avg/min/max finds no fractional input anywhere. Plan §8.2's M9 row explicitly says a noun:Sample aggregate over a 4-decimal field_n 'is added to the suite (B7)'; it was not. test_b7_double_parse pins only rows.py's parse hook, not the arithmetic downstream of it.

**Concrete failure.** I planted `total += Decimal(float(v))` at evaluate.py:395 (the exact-decimal accumulation for sum/avg) and ran the full suite: 568 passed, 10 skipped, 1 xfailed — completely green. With that defect, a pick summing an admissible JSON number like 1000000000000.1 (well below the probe's DBL_MAX threshold) returns 1000000000000.099976 where the true answer is 1000000000000.100000 — a subtly wrong number that runs clean, the project's own named failure mode, invisible to the suite because no test ever aggregates a non-integer. (Reverted; suite re-verified green.)

**Why it survived refutation.** Reproduced end-to-end in a scratchpad copy (worktree untouched). (1) Grep confirms every aggregate pick in demo/tests uses integer inputs — $.payload.load, $.priority, or loadx = payload.load*2 — and no test calls evaluate.aggregate() directly; no fractional value ever flows through the exact-decimal sum/avg accumulation at demo/pyrunner/evaluate.py:395. (2) Applied the claimed mutation `total += Decimal(float(v))` at line 395 in a full copy of the tree and ran the entire suite against the live demo stack: identical results to the unmutated baseline (564 passed + the same 4 copy-only failures caused by the absent .git in my copy; equals the real tree's 568/10/1 green). The suite is blind to the defect. (3) The wrong number reproduces exactly: mutated sum of Decimal("1000000000000.1") = 1000000000000.099976 vs real 1000000000000.100000, and the value is admissible (probe guard threshold ~

### [HIGH] The served page fetches a stylesheet from fonts.googleapis.com on every load, and the AC-32 test that guards this reads only the two files that never contained the URL
`/home/corgea/autoSQL-T-2-build/demo/vendor/styles/watery.css`:8 · lens: isolation-safety

**What is wrong.** demo/static/index.html:34 links /vendor/styles/watery.css, and line 8 of that file is a live `@import url('https://fonts.googleapis.com/css2?family=Inter:...')`. An @import in a linked stylesheet is fetched unconditionally by every browser, and no Content-Security-Policy is set anywhere (grep finds none; `curl -sI` shows none), so nothing blocks it. The build knows about this line — index.html's comment and demo.css:20 both name it — but treats it as harmless because the self-hosted Inter wins the cascade and the request fails offline. The guard test `test_nothing_on_the_page_is_fetched_from_another_host` (demo/tests/test_ui.py:971-982), whose own docstring cites AC-32 and says "a screen that only looks right online is a screen that is wrong", iterates over exactly two files — `index.html` and `demo.css` — neither of which has ever contained the string. It is structurally incapable of failing on the file that actually breaks the promise. Note the fix is not to edit watery.css: demo/tests/test_vendor.py:105 pins it byte-identical to GIMS's `static/styles/watery.css`. It has to be served with the @import stripped, or the page has to stop linking it, or a CSP has to block the host.

**Concrete failure.** Evan opens http://127.0.0.1:8787/ to show the demo — on an employer's network, or anywhere with a DNS resolver. The browser resolves and requests https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap on every page load of a demo presented as fully offline. Meanwhile `pytest demo/tests/test_ui.py -k another_host` reports green.

**Why it survived refutation.** Every factual claim reproduces on the running demo. index.html:34 links /vendor/styles/watery.css; line 8 of that file is a live @import to fonts.googleapis.com, and the demo's own uvicorn serves it verbatim (raw StaticFiles mount, app.py:1029) — confirmed by curl against the live server at 127.0.0.1:8787. No CSP exists anywhere (no middleware, no meta tag, none in the live response headers) and no service worker; a browser fetches an applied stylesheet's @import unconditionally, so the external request happens on every online page load. The guard test test_nothing_on_the_page_is_fetched_from_another_host (test_ui.py ~971-982) iterates over only index.html and demo.css — neither ever contained the URL — so it passes green while the served page carries the import; it cannot fail on the file that breaks the property it is named for. test_vendor.py (~line 105) pins watery.css byte-identical

### [HIGH] run-demo's readiness poll is a second, unfenced connection factory: PGHOSTADDR redirects it off 127.0.0.1, PGSERVICE bricks both `up` and `test`, and the "nothing else imports the driver" test cannot see it
`/home/corgea/autoSQL-T-2-build/run-demo`:125 · lens: isolation-safety

**What is wrong.** `wait_for_db_ready` (run-demo:117-146) embeds its own `import psycopg` / `psycopg.connect(...)`, called by both the `up` and the `test` verbs. It passes only host, port, dbname, user, password, connect_timeout, application_name. It does NOT pass `hostaddr`, does NOT pass `sslmode` or `options`, and does NOT remove PGSERVICE/PGSERVICEFILE — i.e. it omits every one of B13-EXT-1/2/3, the three extensions db.py's own docstring says were measured on this machine and were necessary. db.py:36-46 calls hostaddr "the one parameter whose absence could still send the demo somewhere it did not choose", and this is exactly that absence, in the one file the spec defines as part of the demo tree alongside demo/. The guard that is supposed to catch this cannot: demo/tests/test_isolation.py's `_python_files()` walks `_DEMO_DIR.rglob("*.py")`, so `./run-demo` — a shell script at the repo root — is outside the walk, and `test_only_db_py_imports_the_driver` asserts `importers == ['demo/server/db.py']` over a set that never included it.

**Concrete failure.** Two, both measured. (a) With PGHOSTADDR exported in the shell, the launcher's own connection is dialled at that address on port 55440, carrying the demo's credentials, instead of 127.0.0.1 — the redirect db.py exists to prevent, reintroduced outside db.py. (b) With PGSERVICE (or PGSSLMODE) inherited from the environment, `./run-demo up` and `./run-demo test` spin for the full 60 s and then fail with "autosql-demo-db did not become ready within 60s" plus 40 lines of container log — blaming the container — while the database is healthy and db.connect() reaches it fine. The suite never runs.

**Why it survived refutation.** Every factual element of the claim was verified against the code and reproduced live. run-demo's wait_for_db_ready (lines 117-146, `import psycopg` at line 125) is a second connect path called by both `up` and `test`, passing only host/port/dbname/user/password/connect_timeout/application_name — omitting hostaddr (B13-EXT-1), sslmode/PGSERVICE-removal (B13-EXT-2), and options (B13-EXT-3), the three extensions db.py's docstring records as measured necessities on this machine. Reproduced with the venv python against the running stack: clean env connects to 127.0.0.1:55440; PGHOSTADDR=127.0.0.9 makes the poll dial 127.0.0.9:55440 (the redirect B13-EXT-1 exists to close); PGSERVICE=nonsense and PGSSLMODE=require each make the poll fail unconditionally, while demo.server.db.connect() under all three variables at once still reaches 127.0.0.1:55440 — the fence holds only where it exists. End-to

### [MEDIUM] Overflow numeric literal passes the gate, then crashes as a bare 500 instead of a named refusal
`/home/corgea/autoSQL-T-2-build/demo/builder.py`:293 · lens: gate-safety

**What is wrong.** The gate's num row (gate.py:146-148) accepts any non-bool int/float, including float('inf'), which the vendored parser produces for a literal like 1e999 or 2e308. compile_ast then raises Uncompilable ('numeric literal overflows to inf/nan') at compile.py _t_num — and nothing catches it: builder._compile_expression (builder.py:288-293) catches only ExprError, and run_pick (app.py:894-909) catches only gate.Refused and builder.IllegalPick. The exception escapes the route as HTTP 500 'Internal Server Error'. This breaks three of the build's own rules: plan §10.1 (no refusal may fail to name the construct — a bare 500 is worse than a bare 'invalid'), spec §4.3's contract that gate→compile ordering makes every refusal a visible named event, and gate.py's own header claim that anything not compilable is 'refused, by name, before any SQL exists'. The asymmetry is telling: the gate's field row checks |index| < 2^31 precisely to pre-empt a compile-time failure, but the equally compile-fatal non-finite literal is not checked.

**Concrete failure.** A person at the demo types the filter `$.payload.load > 1e999` in operation 3's box (a natural thing to type at a demo about big-number poison; walkthrough steps 11-13 are all about values near 1e300/1e400). POST /api/pick returns HTTP 500 with body 'Internal Server Error' — no construct, no rule, no refusal payload — instead of the amber layer-1 refusal every other out-of-subset input gets. The screen gets nothing it can render.

**Why it survived refutation.** The finding reproduces exactly as claimed, both in-process and over HTTP. (1) gate.py's num row accepts any non-bool int/float with no finiteness check — verified by reading the tag=='num' branch and grepping the file for inf/nan/finite (nothing). (2) The vendored parser (demo/vendor/expr.py:193) builds ('num', float(v)), so '1e999' becomes inf. (3) The T-1 compiler's _t_num (spikes/T-1/proto/compile.py:204-208) raises Uncompilable('numeric literal overflows to inf/nan...'). (4) builder._compile_expression catches only _expr.ExprError; run_pick catches only gate.Refused, builder.IllegalPick and probes.RuntimeRefusal; the api_pick route and app have no other handler — grep for Uncompilable/exception_handler/middleware across demo/server and demo/tests returns zero hits. (5) In-process repro: builder.build() with filter '$.payload.load > 1e999' let autosql_t1_compile.Uncompilable escape un

### [MEDIUM] Plan §8.2's 16-mutant pass has never run — `./run-demo test --mutants` is unimplemented, and the gap is disclosed nowhere
`run-demo`:363 · lens: test-integrity

**What is wrong.** Plan §8.2 says 'the build runs a mutation pass before it hands over' and risk row 15 (the T-1 dead-failure-branch precedent) leans on it as the proof the catchers work. run-demo:363-366 refuses --mutants with exit 2 ('not wired into this verb yet') — honest, so it cannot pass vacuously — but the build handed over without the pass ever running, and neither demo/EVIDENCE.md, WRAPUP-FOR-EVAN.md nor the punchlist records that §8.2 is outstanding. I hand-ran four of the sixteen (M1 divide-by-3-always, M4 drop `- 'ts'`, M8 banker's rounding, M16 leaked committed write): all were killed, and M6/M12/M13 have in-suite detector proofs. The other nine have never been watched failing, and finding 1 proves a defect on M9's axis survives in a nearby spelling.

**Concrete failure.** A reader of plan §8.2 plus the evidence pack's '568 passed, zero failures' concludes the sixteen catchers were watched working; nine of them never were, and one axis demonstrably has no working catcher. The exact T-1 failure the section was written against — failure branches with zero executions — is the current state for those nine.

**Why it survived refutation.** Every load-bearing fact reproduces. (1) Plan §8.2 (T-2-plan.md:1343-1346) promises verbatim that "the build runs a mutation pass before it hands over" via `./run-demo test --mutants`, with the 16-mutant M1-M16 table; risk rows 1 and 15 (plan:1401, 1415) lean on it as the proof the catchers work, row 15 citing the exact T-1 dead-failure-branch precedent. (2) I executed `./run-demo test --mutants`: it exits 2 with the two refusal lines at run-demo:363-366, exactly as claimed, and a grep of the whole worktree finds no mutation-pass implementation anywhere else — only rationale comments naming mutant numbers in gate.py, evaluate.py, and four test files. (3) The build did hand over: the ticket advanced build → auto-review at 2026-08-23T01:01 (ticket passport + events.jsonl), and §6.4 of the plan defines that advance as the handover, so §8.2's deadline has passed. (4) The gap is disclosed nowh

### [MEDIUM] AC-39(c) is asserted by nothing: a permanent run=False xfail with a stale reason, whose promised W17 replacement was never written
`demo/tests/test_vendor.py`:354 · lens: test-integrity

**What is wrong.** test_ac39c_up_completes_with_both_tree_vars_pointed_at_nothing is xfail(run=False, strict=False) with a reason claiming the stack 'does not exist yet at W2' (false — W4-W17 all landed) and that 'the real proof belongs in test_walkthrough.py (W17)'. No AC-39 test exists in test_walkthrough.py or anywhere else; nothing in the suite or run-demo ever sets AUTOSQL_GIMS_TREE/AUTOSQL_GUTS_TREE to a dead path around an `up`. EVIDENCE.md's Run D incidentally demonstrates the property (568 passed with vars=/nope), but that is a recorded run, not a standing test.

**Concrete failure.** On the owner's machine, where ../GIMS-Project resolves, a future edit that makes `./run-demo up` or a walkthrough step read the GIMS checkout passes every run forever: the xfail never executes, strict=False means it can never even xpass into visibility, and the criterion's hostile condition is never created. AC-39(c)'s 'the demo itself never depends on a tree' silently stops being true with a green suite.

**Why it survived refutation.** I tried to refute this and could not — every factual pillar of the claim verifies against the code and reproduces at runtime.

1. The permanent xfail: /home/corgea/autoSQL-T-2-build/demo/tests/test_vendor.py:343-355 is exactly as described — `@pytest.mark.xfail(reason=..., run=False, strict=False)` over a body that is just `raise NotImplementedError`. I ran it (`pytest tests/test_vendor.py -k ac39 -rax`): it reports `XFAIL ... [NOTRUN] AC-39(c) needs the full stack ... none of which exist yet at W2 ... The real proof belongs in test_walkthrough.py (W17)`. With run=False it never executes and can never fail; strict=False means even an accidental execution could never surface as a failure. This is the suite's sole xfail — the standing "1 xfailed" in every recorded run.

2. The reason is stale: the stack it says "does not exist yet at W2" fully exists — demo/server, builder, the seeded data

### [MEDIUM] `./run-demo down` sends SIGTERM then SIGKILL to whatever process id is in the pidfile, with no check that it is the demo's app
`/home/corgea/autoSQL-T-2-build/run-demo`:457 · lens: isolation-safety

**What is wrong.** `stop_app` (run-demo:453-474) reads `demo/.venv/autosql-app.pid`, tests `kill -0 "$pid"`, and if that succeeds kills it — then escalates to `kill -9` after 10 s. `kill -0` only proves *some* process owns that pid, not that it is this demo's uvicorn. The script already has the right check for this exact question (`app_answers()` at run-demo:220-237, and /proc/PID/cmdline is right there), and does not use it. The pidfile is only removed on a clean `down`, so it outlives the app whenever the app dies any other way.

**Concrete failure.** Evan runs `./run-demo up` and leaves the demo running overnight; the machine reboots (or he Ctrl-Cs the app, or uvicorn is OOM-killed). The pidfile still holds, say, 12345. After a reboot Linux re-issues pids from low numbers densely, so 12345 is very likely alive and belonging to something else entirely. His next `./run-demo down` — or the automatic teardown inside `./run-demo test` — prints "run-demo down: stopping the app (pid 12345)" and terminates that unrelated process. This is the demo disturbing a neighbour on the machine, which is the single thing ops/checks/neighbour-ports.sh promises it never does; that check cannot catch it, because it runs up->down inside one process where the pidfile is never stale.

**Why it survived refutation.** The claim is accurate on every load-bearing point and the failure reproduces with the verbatim code. run-demo:522-524 shows `down` calls stop_app unconditionally; stop_app (453-474) validates the pidfile's pid with nothing but `kill -0` before TERM and an escalation to `kill -9`; `rm -f "$APP_PIDFILE"` exists only inside stop_app, so the file (demo/.venv/autosql-app.pid, plain disk, survives reboot) goes stale whenever the app dies uncleanly. No /proc/cmdline check exists anywhere in the script, and app_answers() (220-237) is never consulted by `down`. I extracted lines 453-474 unmodified, pointed APP_PIDFILE at a scratch file containing the pid of an innocent `sleep 600`, and ran it: it printed "run-demo down: stopping the app (pid 1658490)" and terminated the bystander. The behaviour is not documented as a ruling — "pidfile", "kill -9", and "stop_app" appear nowhere in T-2.md, T-2-plan

### [MEDIUM] The read-only guard is issued after db.connect() has already opened a transaction, so it does not apply to the transaction every pick and every suite read actually runs in
`/home/corgea/autoSQL-T-2-build/demo/server/app.py`:1129 · lens: isolation-safety

**What is wrong.** `db.connect()` calls `_verify()` (db.py:207-242), which runs five `conn.execute(...)` statements before the caller ever sees the connection. With `autocommit=False` (the default) that opens a transaction that stays open. Callers then issue `SET SESSION CHARACTERISTICS AS TRANSACTION READ ONLY`, which sets `default_transaction_read_only` for *subsequent* transactions and has no effect on the one already in progress — which is the transaction every statement of the pick, and of the whole session-scoped suite fixture, then runs in. Same defect at demo/tests/conftest.py:40 and :124, demo/tests/test_ui.py:50, demo/tests/test_order.py:355, demo/tests/test_walkthrough.py:398/1259/1401/1428/1474. Nothing asserts the guard actually holds. The correct statement is `SET TRANSACTION READ ONLY` (current transaction), or setting the parameter inside connect() before `_verify` runs.

**Concrete failure.** app.py:1128's own comment — "The app reads; it never writes. A pick cannot alter the seeded rows AC-10's digest is taken over" — and conftest.py:9's "nothing that reads demo.records through it can write, which is the cheap half of B10's protection" are both false as written. The seed is protected today only by the accident that nothing calls commit(); the declared guard contributes nothing. One `db.connect(autocommit=True)` — which the suite already uses at test_walkthrough.py:1461 — or one stray commit, and a write lands for real. B10's digest guard would then report the corruption after the fact rather than the cheap half preventing it.

**Why it survived refutation.** I tried to refute this and could not — every element reproduces against the real code on the demo's own database (55440). (1) db.py connect() defaults autocommit=False and _verify() (db.py:207-242) runs five conn.execute() calls before returning, with no commit/rollback; reproduced conn.info.transaction_status == INTRANS immediately after connect(), so a transaction is already open when app.py:1129 / conftest.py:40 issue their SET. (2) Following api_pick's exact sequence: SHOW transaction_read_only -> off, SHOW default_transaction_read_only -> on, and UPDATE demo.records SET key = key WHERE false was ACCEPTED (I rolled it back, nothing committed) — SET SESSION CHARACTERISTICS only affects subsequent transactions, and since nothing ever commits, the entire pick and the entire session-scoped suite fixture run inside the pre-existing read-write transaction. (3) The proposed fix works: SET T

### [LOW] A numeric literal that overflows to inf passes the gate and crashes as a bare 500 instead of a named refusal
`demo/gate.py`:146 · lens: sql-arithmetic

**What is wrong.** gate.py's num-tag check accepts any float — including inf/nan — (`isinstance(node[1], (int, float))`, no finiteness check), so an expression containing the literal 1e400 or 1e309 passes layer 1. The pinned compiler then raises Uncompilable (compile.py _t_num: 'numeric literal overflows to inf'), and run_pick (demo/server/app.py:886-909) catches only gate.Refused and builder.IllegalPick — Uncompilable escapes to a bare HTTP 500 'Internal Server Error'. The demo's own doctrine (plan §10.1) is that every refusal is named; this input, typeable into the op-2/op-3 expression box, gets no refusal at all.

**Concrete failure.** POST /api/pick with computed [{name: c1, expr: "1e400"}] → HTTP 500, empty body. Same for "$.a * 1e309". A person exploring the demo's own big-number story (its EdgeCase collection invites exactly that) by typing the literal instead of reading it from a field gets a dead screen instead of the named refusal every neighbouring case produces. No wrong number — the failure is loud — but it is a missed refusal on the demo's signature theme.

**Why it survived refutation.** Reproduced live and traced end to end. gate.py:147 accepts any float with no finiteness check; demo/vendor/expr.py:193 parses "1e400"/"1e309" to ("num", float(v)) = ("num", inf); gate.gate() passes the inf node (verified by direct execution); the pinned compiler's _t_num (spikes/T-1/proto/compile.py:204-209) raises Uncompilable; builder catches only ExprError, run_pick catches only gate.Refused/builder.IllegalPick/probes.RuntimeRefusal, api_pick has no handler, and no FastAPI exception handlers or middleware exist — so Uncompilable escapes to a bare HTTP 500. Confirmed by curl: "2 + 2" → 200; "1e400" → 500 "Internal Server Error"; "$.payload.load * 1e309" → 500; each failing request individually correlated to an autosql_t1_compile.Uncompilable traceback in demo/.venv/autosql-app.log. Reachable from the screen: the picker's expression textarea sends raw text (expr: x.target.value), and th

### [LOW] float8 overflow inside expression arithmetic on seeded data crashes the pick as an unhandled 500
`demo/server/app.py`:960 · lens: sql-arithmetic

**What is wrong.** The pinned compiler documents KNOWN_DIVERGENCES.float8_overflow_raises: + - * / on float8 raises 'value out of range: overflow' in Postgres where Python returns inf. The demo leaves this unhandled: sql_pane's execute (app.py:960 via run_pick) has no handler for the database error, so the pick answers a bare HTTP 500. Reachable from the screen with seeded data: edge-04's g = 1.7976931348623156e+296 is deliberately just BELOW the 297-digit guard, so xpr.num passes it through as a real float8, and squaring it overflows.

**Concrete failure.** POST /api/pick with computed [{name: sq, expr: "$.g * $.g"}] on noun:EdgeCase → HTTP 500 'Internal Server Error'. The Python pane would have answered ~3.23e592 (a float, finite); neither a comparison nor a named layer-2 refusal is shown — the request just dies. Follow-up picks are unaffected (per-request connection, closed in finally; verified by running a normal pick immediately after — it answered correctly), so this is a one-off crash, not state poisoning.

**Why it survived refutation.** Reproduced the exact claimed failure against the running demo stack (127.0.0.1:8787). POST /api/pick with {"source":"noun:EdgeCase","computed":[{"name":"sq","expr":"$.g * $.g"}]} returned HTTP 500 with the bare body "Internal Server Error", and the app log (/home/corgea/autoSQL-T-2-build/demo/.venv/autosql-app.log) shows the cause: psycopg.errors.NumericValueOutOfRange: value out of range: overflow, raised inside cursor.execute. Every link in the claimed chain holds in the code: (1) the gate allows "*" (ALLOWED_BIN_OPS = {"+","-","*","/"}, spikes/T-1/proto/compile.py:78); (2) the compiler renders "*" as bare float8 multiplication, to_jsonb(xpr.num(l) * xpr.num(r)) (compile.py:284-285), and KNOWN_DIVERGENCES.float8_overflow_raises (compile.py:73-84, guarded:false) records exactly this raise; (3) seeded edge-04 holds g=1.7976931348623156e+296 (demo/seed/generate.py:198-199), deliberately j

### [LOW] Malformed pick shapes crash /api/pick and /api/operations with bare 500s instead of DR-2 refusals
`/home/corgea/autoSQL-T-2-build/demo/legality.py`:205 · lens: gate-safety

**What is wrong.** legality.evaluate's tolerant readers assume dict-shaped holders: _agg_fn does pick.get('aggregate').get('fn') after `or {}` — but a truthy non-dict ('sum') passes the `or` and then .get raises AttributeError (legality.py:205); same for sort ('ts'.get at legality.py:318) and window (builder.py rolling_column's .get). run_pick's alias loop (app.py:873-875) does cc.get('name') on every element of pick['computed'] with no isinstance check, so computed='notalist', computed=42, or a stray string inside the list raises AttributeError/TypeError. None of these are caught: both routes answer HTTP 500 'Internal Server Error'. The build's own discipline says otherwise in three places: errors.illegal_pick exists precisely for 'a pick arriving other than through the screen... answered rather than quietly repaired' (DR-2); normalised_pick already guards `isinstance(holder, dict)` for exactly these slots but then tolerates-and-forwards the bad value into evaluate; and _pick_from_query's docstring (app.py:1060-1067) records that an unhandled 500 for a malformed pick 'with nothing a screen or a reader could act on' was already measured and deemed a defect — the fence it added covers only JSON-ness, not shape.

**Concrete failure.** POST /api/pick with {'pick':{'source':'noun:Heartbeat','aggregate':'sum'}} (or sort:'ts', window:'payload.load', computed:'notalist', computed:42, computed:[{...},'stray']) returns HTTP 500 'Internal Server Error' instead of a 422 carrying the matrix's words. GET /api/operations?pick={'source':'noun:Heartbeat','sort':'ts'} — the route whose contract the screen re-derives controls from — also 500s. Not reachable from the screen, but /api/pick is the surface Q25's 'anyone can re-slice' invites and the suite drives (run_pick's own docstring: 'the suite drives the same code the screen does').

**Why it survived refutation.** Reproduced end-to-end against the running stack. All six malformed POST /api/pick bodies from the claim ({"aggregate":"sum"}, {"sort":"ts"}, {"window":"payload.load"}, computed:"notalist", computed:42, computed:[{...},"stray"]) return HTTP 500 "Internal Server Error", and both malformed GET /api/operations picks (sort:"ts", aggregate:"sum") return 500. The code paths are exactly as claimed: legality.py:205 (_agg's `or {}` passes truthy non-dicts to .get), legality.py:318 (s.get on a string sort), normalised_pick (app.py:702) isinstance-guards the same slots but forwards the bad value into evaluate, and run_pick's alias loop (app.py:871-874) calls cc.get("name") with no shape check. No global exception handler exists — the sole `except Exception` (app.py:821) is confined to the fallback Python pane. The reviewer's contrast cases also reproduce: bucket:["hour"] and filter:["$.a"] come back

### [LOW] The no-tolerance grep test promised by plan §8.1 row 3 does not exist
`demo/tests/test_isolation.py`:469 · lens: test-integrity

**What is wrong.** Plan §8.1 row 3: 'A grep test fails the build if math.isclose, pytest.approx, abs( … < … e- or rel_tol appears anywhere under demo/.' No such test exists — the suite has watched-detector companions for AC-3, AC-37, B21 and the only-importer check (all in test_isolation.py), but the tolerance grep was never built. I verified by my own grep that the property currently holds: no isclose/approx/rel_tol/epsilon comparison anywhere under demo/ outside .venv; the only bands are the two spec-sanctioned ones (AC-8's 88-92%, AC-40(a)'s 700-1100), each backed elsewhere by exact equalities (861, 8400, 400207).

**Concrete failure.** A future test that wraps a flaky comparison in pytest.approx or abs(a-b)<1e-9 lands silently, and plan §8.1 row 3 names that as the exact mechanism by which the demo's target defect gets hidden. The rule currently holds only by discipline, with nothing to fail the build when it stops.

**Why it survived refutation.** The plan promises this guard twice — §8.1 row 3 names the exact needles ("math.isclose, pytest.approx, abs( … < … e- or rel_tol … anywhere under demo/") and §8.3 restates "No tolerance. §8.1 row 3, enforced by grep." No such test exists anywhere: test_isolation.py's only needle sets are _AC3_FORBIDDEN (live-DB strings), _B21_CLIENT_BINARIES, and the AC-37 timing-vocabulary sweep; I also searched for obfuscated needles in the file's own split-literal style ("is" "close"), subprocess-based greps, and checks in run-demo, scripts, and CI — nothing implements it, and the M1–M16 mutation table does not cover it. No recorded ruling supersedes the promise, even though this build provably records such rulings when a planned grep is changed (B33/W16 did exactly that for B21's grep), and EVIDENCE.md never claims the test exists. It is not either of the two known-and-accepted items. The reviewer's s

### [LOW] AC-31's sweep records step 6's row_count as 'observed' using step 2's row count — a comparison that cannot fail against the claim it covers
`demo/tests/test_walkthrough.py`:1080 · lens: test-integrity

**What is wrong.** In the `observed` fixture, o["steps[5].expect.row_count"] (step 6's 'all 8,400 rows contribute to the sum') is filled from ran[2] — step 2's plain-select row count — not from anything step 6 returns. The sweep's own contract is that every entry is either genuinely observed from the running demo or on NOT_APP_OBSERVABLE with a reason; this entry is nominally observed but observes a different pick, so it passes regardless of step 6's behaviour. (The sum value itself on line 1079 is a real observation and is what actually holds step 6 — rows silently dropped from the sum would move that number — so no wrong number gets through today; the sweep's accounting is what is wrong.)

**Concrete failure.** If the SQL numeric read ever silently excluded rows from step 6's aggregation in a way that happened to preserve the total (e.g. excluding only zero-valued loads — 74 seeded rows have load 0), the sum comparison stays green and the row_count 'observation' cannot fire because it is reading step 2. The entry claims coverage the sweep does not have.

**Why it survived refutation.** Verified every load-bearing element against the worktree and the running demo. (1) test_walkthrough.py:1080 verbatim fills o["steps[5].expect.row_count"] from ran[2] — STEP_PICKS[2]'s plain select — not from step 6's aggregate; confirmed by reading lines 1075-1082 and the STEP_PICKS table. (2) The file entry's claim is aggregation-specific: its derivation (expected-answers.json and EVIDENCE.md:1280) says "none drops out of the sum," yet step 6's response is a single ["agg"] scalar with no contributing-row count, so the claim is unobservable from step 6 — and the emitted SQL's CASE WHEN jsonb_typeof(...)='number' numeric read is exactly a construct where rows CAN silently drop out of SUM. (3) The concrete failure reproduces: I ran step 6's real builder-emitted SQL against the demo DB (55440), then a mutant excluding zero-load rows; both return the identical Decimal('400207.000000'). The s

### [LOW] A literal cannot-fail assertion in test_decimal.py
`demo/tests/test_decimal.py`:103 · lens: test-integrity

**What is wrong.** `assert getcontext().rounding != ROUND_HALF_UP or True` is `X or True` — vacuously true under every possible state, the only assertion of the tautology shape found in the suite (it is even self-labelled 'documentative'). The two real assertions above it in the same test (q6 correct under a hostile localcontext) are genuine and were verified.

**Concrete failure.** The line asserts that the ambient decimal context was left untouched after q6 ran; if q6 ever started mutating the global context, this line would still pass. The property it names is unchecked.

**Why it survived refutation.** Verified at test_decimal.py:103: the line is literally `assert getcontext().rounding != ROUND_HALF_UP or True` — vacuously true, and the only such tautology in demo/tests (grep confirmed). I tried to refute it on the theory that localcontext()'s exit-restoration makes the property uncheckable there anyway (so the `or True` would cost nothing), but an experiment disproved that: with an evil q6 that mutates the global decimal context while returning correct numbers, run in the file's real test order (earlier tests call q6 outside any localcontext, so pollution persists to line 103), the shipped assertion passes while the same line without `or True` fails — the property IS checkable at that point and the `or True` is exactly what disarms it. With the real q6 the de-tautologized line also passes, so the tautology guards against nothing. The reviewer's concrete failure reproduces; the 'docume

### [LOW] start_app declares success when anything at all is listening on 8787, and can record a dead pid over a live orphan
`/home/corgea/autoSQL-T-2-build/run-demo`:442 · lens: isolation-safety

**What is wrong.** `start_app`'s readiness loop polls `port_in_use "$APP_PORT"`, which only proves a socket is bound, not that it is this demo's uvicorn. The script already carries the correct check for that question — `app_answers()` at run-demo:220-237, which asks /api/operations — and `prepare_stack_for_test` uses it; `start_app` does not.

**Concrete failure.** Two `./run-demo up` runs racing (two terminals, or a forgotten background one) both clear `guard_ports`. The second's uvicorn exits immediately with "address already in use", but `echo $! > pidfile` has already recorded its pid and `port_in_use` sees the *first* app's socket — so `up` prints "the screen is up" and the pidfile now holds a dead pid. The next `./run-demo down` finds `kill -0` fails, silently removes the pidfile, and tears down the database — leaving the first uvicorn orphaned on 8787 with no pidfile to kill it and no database behind it, and every subsequent `up` refusing the port. Requires the race; the logic gap is unconditional.

**Why it survived refutation.** Reproduced the core mechanic live against the running stack rather than just reading it. With the demo's own uvicorn (pid 1642298) already holding 8787, I ran start_app's launch subshell verbatim from /home/corgea/autoSQL-T-2-build/run-demo:435-438 (pidfile and log redirected to scratch so the real stack was untouched): the second uvicorn died in ~0.5s with '[Errno 98] address already in use' (exit 3), the recorded $! was dead by the second poll, and port_in_use(8787) still returned 0 — so the readiness loop at run-demo:442 would print 'the screen is up' and return success exactly as claimed. The rest of the chain holds by direct code check: both invocations write the same absolute pidfile (run-demo:410, written unconditionally at 438, clobbering the live pid with a dead one); stop_app (457-472) finds kill -0 failing on the dead pid, skips the kill silently (the echo is inside the succes

## Refuted — did NOT reach the owner
- **/api/pick returns answers with no invented-data label, and the page itself sends people there** — The reviewer treated a deliberately-scoped labeling ruling as a broken safety rule. B31/AC-11 define the label surfaces exhaustively (masthead chip+banner, per-pane chip, seed console line) and B25 pins the /api/pick JSON envelope with no label field — the build implements both exactly. The finding's only spec-independent failure path, the noscript pointer, does not reproduce: the GET endpoints a 

## What was checked and found sound
- sql-arithmetic: B11 bind namespacing holds in every statement shape, verified live: shape A with 2 computed columns + filter + window-on-cc emitted cc0_*/cc1_*/flt_*/win_* binds; shape B (CTE) the same; shapes C/D re-emit the aggregate field's fragment under agg_* — in each case the set of %(...)s placeholders in the executed SQL exactly equals the set of bound names, merge_params refuses any collision, and probe fragments use their own prbA*/prbB* prefixes. The *_ctx surface is dead code in practice (now/today are outside the gate's 7-function allowlist) and would be consistent anyway (shared setdefault fill).
- sql-arithmetic: B1 (one bucket column): shape D emits exactly one "bucket" (the to_char label; date_trunc carries no alias), GROUP BY/ORDER BY "bucket" resolve to it unambiguously; bucket-by-day counts (7 × 1200) verified against an independent text-truncation query (substr(ts,1,10)), hour buckets spot-verified; TimeZone is pinned UTC, SET and read back on every connection (db.py B13-EXT-3), so date_trunc truncates in UTC as the Python pane does.
- sql-arithmetic: B2 (no alias in aggregates): shapes C and D re-emit the computed column's compiled expression inline inside the numeric read; live picks aggregating over a cc (sum/avg, scalar and bucketed, with filter) run clean and agree digit-for-digit; the cc alias does not appear in those statements at all.
- sql-arithmetic: B3 (the CTE): filter inside, LIMIT outside, changed computed-but-not-emitted, IS DISTINCT FROM keeps first beats. Hand-verified independently: my own script computed rolling averages and changed flags for sender hb-01 straight from data::text — 16 kept rows, every rolling_avg digit-for-digit identical to the pane's answer.
- sql-arithmetic: Rounding and precision: ops 6/7/8 stay in numeric end to end (sum($.a) returns the exact 307-digit 1e300 total; edge-04/05 sums exact). round(numeric,6) is half-away-from-zero (measured: ±0.0000005 → ±0.000001), matching q6's ROUND_HALF_UP; q6 refuses floats/bools/non-finite and carries its own prec-1000 context, as do the pyrunner's sum/avg accumulations, so no ambient-context or 28-digit truncation path exists. Ties at the 6th place are unreachable with the seeded value scales, and PG's avg keeps ≥20 fractional digits before the final round.
- sql-arithmetic: The probe boundary holds from both sides: sum($.huge) (1e400) refused as member (a) naming edge-03; sum($.a) (1e300), sum($.g) (~1.79e296) and sort-by-$.huge accepted; a filter touching $.huge is refused (the probe deliberately ignores the pick's filter); sum($.d) accepted (probe scope = touched operands only); max($.l) over [1e300,1] is NOT refused and shows the asserted disagreement (SQL 1 vs Python 1e+300); == on containers refused as member (b); the 297-digit-guard boundary is visible exactly as designed (edge-04 agrees, edge-05 shows SQL null vs Python 1.797…e+296). The probe reads raw jsonb through ::numeric, never xpr.f8/num. The numeric-context enumeration is sound for the 7 gated functions (abs/min/max args probed; if/coalesce evaluated whole and lazily; count/length correctly not numeric contexts — a max($.huge, 1) pick is caught by the probe before Python's OverflowError could ever race it).
- sql-arithmetic: Ordering, apart from the empty-array finding: the comparator matches real jsonb btree order over a 27-value crafted set (nested arrays, object pair/storage-order rules, bool>number>string>null, numeric order across 301-digit values); full-result sorts of all 2000 noun:Sample rows on three mixed-type field_N columns in both directions — zero differing cells; the two null bands ($.n asc/desc on EdgeCase) agree; datcollate=C confirmed, key tiebreak byte order, NULLS LAST unconditional both directions.
- sql-arithmetic: Comparison integrity: exact jsonb reads on both sides (parse_float=Decimal), type-tagged canonical values (bool never equals 1), kinds taken from the executed cursor and applied to both panes, no tolerance anywhere; a hostile alias (x"; DROP TABLE …) is refused by name before SQL exists and demo.records still holds 10,410 rows; field slots that the two panes would read differently are refused (bracket/index spellings).
- sql-arithmetic: Session/isolation on the paths I touched: per-request connection from the one factory, SESSION READ ONLY, closed in finally — an aborted statement does not poison later picks (verified live).
- sql-arithmetic: The suite's claimed numbers reproduce: 568 passed, 10 skipped (GIMS trees absent), 1 xfailed, 46s. Note for the record: during this review two uncommitted mutations briefly appeared in the worktree (rolling divisor → Decimal(3); COMPARED_EXPR without - 'ts' — the exact named mutants M1/R13) and were reverted by whatever planted them; I re-verified the tree is byte-identical to HEAD afterwards, and the running server predates them, so no live result in this review was produced by mutated code.
- gate-safety: The 32-construct allowlist genuinely holds under structural attack: fuzzed gate() with 200,000 random hostile ASTs (invalid tags like 'exec'/'subquery', hostile operators %, **, ||, IN, LIKE, out-of-subset and unknown function names, hostile field steps, bool-as-num, 2^31 indexes, injection strings in str/field positions). 25,569 were accepted, and every accepted tree either compiled to SQL containing none of the out-of-subset runtime calls (xpr.fmod, xpr.round, xpr.pdate_ms, lower(, upper(, etc.) or raised only the known inf-literal Uncompilable (finding 1). Zero gate crashes, zero refusals lacking a named construct and rule. The |index| < 2^31 boundary is exact in both directions (2147483647/-2147483647 pass, 2147483648/-2147483648 refused by name).
- gate-safety: All 16 refused constructs refuse BY NAME end-to-end: POSTed each one ('round', '%', 'floor', 'ceil', 'sum', 'avg', 'string', 'number', 'concat', 'contains', 'lower', 'upper', 'today', 'now', 'days_between', 'date_add') as a live filter — every refusal names the construct and says 'outside the safe subset' with the seven allowed functions listed; an unknown 23rd function is refused by the parser ('Unknown function') before the gate, which is defense in depth on top of the gate's own fail-closed branch.
- gate-safety: The legality matrix's enabled side is genuinely legal Postgres: swept 7,776 pick combinations (3 sources x computed x filter x 3 sorts incl. alias-sort x cap x 6 aggregates x 3 granularities x 3 windows incl. alias-window x changed); evaluate() called 332 legal, and all 332 built AND EXECUTED cleanly against the live demo database on 55440 — including the hard corners: CTE shape with named-window rolling_avg + changed + alias sort + cap, BUCKET with computed-column aggregate re-emitted inline, GROUP BY/ORDER BY "bucket" resolution. No enabled-but-illegal combination exists. The disabled side errs only in the safe direction (cap/sort on SCALAR are legal-but-pointless; 42803 combinations are correctly disabled with the SQLSTATE named in the reason). test_legality.py's 3,456-combination walk checks the full enabled set, every reason's wording, and the contract mirror against a hand-written oracle — a test that can genuinely fail.
- gate-safety: The alias validator survives attacks past its 13-name table: case variants (Data, KEY, Collection — distinct quoted identifiers, no collision), JS-hostile names (__proto__, constructor), SQL scope names (r, w, picked, SELECT, changed_flag) all validate, emit quoted, and ran end-to-end to 'agree' verdicts — including sorting and windowing ON those aliases in the CTE shape where they sit next to WINDOW w and the picked CTE. The 63-char boundary holds (64 refused, naming NAMEDATALEN truncation), fullmatch-not-match is both used and mutation-tested (M13), unicode/quotes/%/spaces are all refused by the ASCII-only pattern with the rule stated, and validation always precedes quoting via the single emit_alias path — the validated-and-quoted string is the only form that reaches SQL text.
- gate-safety: No path bypasses the gate: every compile_ast call site receives either gate-approved trees (builder._compile_expression enforces parse -> gate -> compile in that order for op 2 and op 3; probes receive the same already-gated trees re-parsed from the same source, after build succeeded) or parser-generated plain field paths fenced by _as_dollar_path (W13-2 refuses '$[...]' and quote-bearing slots by name at layer 1). Every conn.execute site in the tree (sql_pane, probes, pyrunner rows, db session pins, collection_keys/numeric_fields) is a parameterized statement or a fixed string; /api/pick sets the session READ ONLY; user-typed text reaches SQL text nowhere except through emit_alias.
- gate-safety: operations.py invents no rule: the contract is a pure merge of legality.evaluate's answer with static control descriptions; disables propagate to child controls with the same reason; op 6 loses the 'none' option on BUCKET matching B5c's one-way transition; op 7's transition rule is stated in the payload rather than silently applied.
- gate-safety: Observation (not a finding): mid-review, demo/builder.py transiently changed on disk (COMPARED_EXPR became "r.data" — a mutant that would make operation 9 keep every row) and reverted to the committed "r.data - 'ts'" minutes later, and the app on 8787 was bounced and restarted by an external process. Final state matches HEAD 8bb9018, git status clean — consistent with a concurrent mutation-testing or harness process, but worth the owner knowing the worktree was being written to during review. The stack was left running as instructed.
- test-integrity: The third path's arithmetic is right, verified by a genuinely independent fourth computation: I re-derived every headline walkthrough number from generate.py's rows using pure integer arithmetic (no Decimal, my own half-up-at-6 via divmod) — total 8400, true/false 7543/857, first non-ok hb-01-0148/'warn', step-5's ten keys, sum 400207, 7 buckets of 1200, kept 861 with first five hb-01-0000/0006/0007/0041/0056, worked sender uniquely hb-18 with loads [27,88,88,88,88] and values 27.000000/57.500000/67.666667/88.000000/88.000000, and the sha256 over all 8,400 rolling cells — every one byte-identical to demo/expected-answers.json.
- test-integrity: Hand-computed literals in the tests check out by hand: 203/3=67.666666... rounds up to 67.666667, 61/3=20.333333, 115/2, (6+9)/2=7.5 with the null-frame divisors, and test_decimal's tie table (half-up vs half-even shown side by side, the non-distinguishing 41999.9999995 row correctly filtered out of the M8-killing set, negative ties away from zero).
- test-integrity: B8's independence is real, not aspirational: expectations.py defines its own q6/numeric_read/ordering (no shared helper with pyrunner/builder/probes), the AST test catches six spellings of the forbidden import plus the importlib/__import__ back door, the detector is exercised on positive samples, and expected-answers.json is regenerated in-process and compared byte-for-byte every run.
- test-integrity: No tolerance anywhere, verified by reading and by grep rather than trusted: every numeric assertion is exact Decimal/int/string equality; the only two bands are the spec's own (AC-8 88-92%, AC-40(a) 700-1100) and each band-checked number is also asserted exactly (861, 8400) in the same or a neighbouring test; _agrees() in test_walkthrough is exact-Decimal equality, not a tolerance, and rejects bool==1.
- test-integrity: The suite genuinely kills the mutants I planted: divide-by-3-always failed 4 named tests including the 8,400-cell digest; banker's rounding failed 19; dropping the `- 'ts'` exclusion in builder.py turned the suite red at collection; a deliberately leaked committed write made B10's checksum guard print LEAKED and force pytest's true exit status to 1 (verified unpiped). All plants reverted; tree clean, DB digest restored to the manifest's md5, final full run 568/10/1 green.
- test-integrity: The three-way comparison's coverage sweep is airtight in shape: observed ∪ NOT_APP_OBSERVABLE is asserted to equal the file's 59 entries exactly, every NOT_APP_OBSERVABLE justification checks out on inspection, and the previously carved-out step-13 value ('inf') is now a real observation ('raised') with its mechanism (401-digit jsonb integer literal -> exact int -> OverflowError) asserted at source against the real row and the vendored evaluator.
- test-integrity: AC-22's asserted disagreement is a real both-sides assertion (Python 1e+300, SQL 1, edge-01, exactly one differing row, cell-level diff marks, reproduced five times), not a 'they differ somehow' check — agreement there fails the build.
- test-integrity: The order comparator's hand-written fixture matches Postgres's documented jsonb ordering band for band (Null<String<Number<Boolean<Array<Object, storage-order interleaved object comparison including the docs' own {"aa":1,"c":1} example), the descending sequence keeps NULLS LAST and key-ASC tiebreaks, and the three-band Sample sort's 41/138/1821 split was verified independently against the generator.

FINDING — 16 confirmed defects, 4 of them HIGH — sending back to build. The headline one inverts the demo's own signal: a UI-reachable pick makes the disagreement banner fire when the generated SQL is RIGHT and the independent Python control is WRONG.
EVIDENCE: 4-lens adversarial review of branch feat/T-2-demo at 8bb9018 (101 files, ~25,500 lines) — 16 defects confirmed by a refuting verifier each, 1 refuted before reaching the owner; 4 HIGH (the jsonb top-level empty-array ordering divergence that makes the Python control cry wolf, the fonts.googleapis.com fetch breaking AC-32's offline promise with a guard test that cannot see it, run-demo's unfenced second connection factory redirectable by PGHOSTADDR, and an all-small-integer aggregate corpus that lets a float-contaminated aggregate pass all 568 tests)
